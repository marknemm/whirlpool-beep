/**
 * Configuration options for the `Config.init` method.
 */
export interface ConfigInitOptions {

  /**
   * Whether to suppress logging the environment variables.
   *
   * @default false
   */
  suppressLogEnv?: boolean;

}
