import { type Position, type Whirlpool } from '@orca-so/whirlpools-sdk';

/**
 * Get a {@link Whirlpool} {@link Position}.
 *
 * @returns A {@link Promise} that resolves to the {@link Position} in the {@link Whirlpool}.
 */
export async function getPosition(): Promise<Position | null> {
  // PDAUtil.getPositionBundle();

  return null;
}
