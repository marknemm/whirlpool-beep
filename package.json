{
  "name": "npc-beep",
  "version": "0.0.1",
  "description": "NPC Beep-Boop-Bop 🤖",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "clean:all": "npm run clean:all --workspaces --if-present",
    "clean:deps": "npm run clean:deps --workspaces --if-present",
    "clean:dump": "npm run clean:dump --workspaces --if-present",
    "clean:logs": "npm run clean:logs --workspaces --if-present",
    "dev:package": "npm run dev:package --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "prepare": "ts-patch install -s",
    "prod:package": "npm run prod:package --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.5.0",
    "@types/bn.js": "^5.1.5",
    "@types/eslint__js": "^8.42.3",
    "@types/node": "^20.14.8",
    "@types/yargs": "^17.0.32",
    "dotenv-cli": "^7.4.2",
    "esbuild": "^0.20.2",
    "eslint": "^8.57.0",
    "eslint-plugin-import-alias": "^1.2.0",
    "eslint-plugin-jsdoc": "^48.5.0",
    "eslint-plugin-no-floating-promise": "^2.0.0",
    "globals": "^15.6.0",
    "rimraf": "^5.0.7",
    "serverless": "^4.1.21",
    "serverless-plugin-common-excludes": "^4.0.0",
    "ts-node": "^10.9.2",
    "ts-patch": "^3.2.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.5.2",
    "typescript-eslint": "^8.0.0",
    "typescript-transform-paths": "^3.5.0",
    "utility-types": "^3.11.0"
  },
  "author": "Mark Nemmer",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0",
    "yarn": "Use npm!"
  },
  "workspaces": [
    "libs/core",
    "libs/db",
    "libs/solana",
    "libs/*"
  ]
}
